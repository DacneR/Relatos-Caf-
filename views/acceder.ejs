<% if (mensaje && mensaje.length > 0) { %>
    <div class="mensaje-flash">
        <p><%= mensaje %></p>
    </div>
    <% mensaje = null; %>
<% } %>
<main>
    <br>
    
    
    <center>
        <form action="/login" method="POST">
            <div class="Dform">
                <img src="img/logo-trans.png">
                    <br>
                <input type="text" placeholder="Correo" id="correo" name="correo" required>
                    <br> 
                    <br>
                <input type="password" placeholder="Contraseña" id="clave" name="clave" required>
                    <br>
                <input type="checkbox" id="showPassword" onclick="togglePassword()"> Mostrar clave
                    <br>

                <!-- Botón de envío -->
                <input type="image" src="img/botones/boton-ingreso.png" alt="Ingresar" class="Dboton">
                <!-- Botón para registro (redirigir a otra página, por ejemplo) -->
                <a href="/registro">
                <img src="img/botones/boton-registrarse.png" alt="Registrarse" class="Dboton">
                </a>
            </div>
        </form>
    </center>
</main>